<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | <%= appName %></title>
  
  <!-- Bulma CSS Framework -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
  
  <!-- Custom styles -->
  <style>
    .navbar-brand .navbar-item {
      font-weight: bold;
      font-size: 1.2rem;
    }
    .risk-badge {
      display: inline-block;
      padding: 0.25rem 0.75rem;
      border-radius: 4px;
      font-weight: bold;
      font-size: 0.875rem;
    }
    .risk-normal { background-color: #48c774; color: white; }
    .risk-at-risk { background-color: #ffdd57; color: #363636; }
    .risk-high { background-color: #f14668; color: white; }
    .question-card {
      margin-bottom: 1.5rem;
      border-left: 4px solid #3273dc;
    }
    .bilingual-text {
      margin-bottom: 0.5rem;
    }
    .bilingual-text .english { font-weight: 600; }
    .bilingual-text .filipino { font-style: italic; color: #7a7a7a; }
  </style>
</head>
<body>
  <%- include('../partials/nav') %>
  
  <main class="section">
    <div class="container">
      <%- include('../partials/flash') %>
      
      <%- body %>
    </div>
  </main>
  
  <%- include('../partials/footer') %>
</body>
</html>
